{
    $number = 12

    ?(number > 0 && number < 20) {
        %>> "Number between 0 and 20"
    }

    ?(number == 0 || number == 12) {
        %>> "Number is 0 or 12"
    }
}

?(12 == 12 || 12 == 0) {
    %>> "Number is 0 or 12"
}